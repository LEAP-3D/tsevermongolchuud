{
  "name": "tsevermongolchuud",
  "private": true,
  "scripts": {
    "setup": "npm install && npm --prefix apps/frontend install && npm run db:generate",
    "setup:with-db": "npm run setup && npm run db:push",
    "lint": "npm --prefix apps/frontend run lint",
    "build": "npm --prefix apps/frontend run build",
    "db:generate": "npm --prefix apps/frontend run db:generate",
    "db:migrate": "npm --prefix apps/frontend run db:migrate",
    "db:push": "npm --prefix apps/frontend run db:push",
    "db:studio": "npm --prefix apps/frontend run db:studio",
    "history:backfill:tz": "node scripts/backfill-history-visitedat.cjs"
  },
  "devDependencies": {
    "prisma": "^6.19.2"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2"
  }
}
